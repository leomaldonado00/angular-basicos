import { Component } from '@angular/core';

@Component({
  selector: 'app-root', // nombre del componente (en este caso el de aplicacion principal) es el componente que se muestra en el index.html
  templateUrl: 'app.component.html', // path del html que corresponde al componente (en este caso misma carpeta)
})
export class AppComponent {

}
